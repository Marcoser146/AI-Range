{
  "name": "ai-range-ai-gateway",
  "version": "1.0.0",
  "description": "AI Gateway Service - integrates an existing AI decision engine into the AI-Range event plane via REST and an event-bus adapter",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "dotenv": "^17.4.2",
    "express": "^4.21.2"
  },
  "optionalDependencies": {
    "pg": "^8.13.1",
    "nats": "^2.29.2"
  },
  "devDependencies": {
    "supertest": "^7.1.3"
  },
  "engines": {
    "node": ">=18"
  }
}
